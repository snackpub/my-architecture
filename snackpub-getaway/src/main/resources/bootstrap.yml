server:
  port: 80

spring:
  cloud:
    discovery:
      reactive:
        enabled: false
    gateway:
      discovery:
        locator:
          enabled: true
    loadbalancer:
      retry:
        enabled: true

# 聚合文档配置
blade:
  document:
    resources:
      - name: 授权模块
        location: /snackpub-auth
      - name: 工作台模块
        location: /snackpub-desk
      - name: 系统模块
        location: /snackpub-system


